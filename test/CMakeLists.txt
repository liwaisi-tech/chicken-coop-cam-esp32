# Test directory for ESP-IDF project components
# This allows running unit tests with 'idf.py test'

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(chicken-coop-cam-tests)